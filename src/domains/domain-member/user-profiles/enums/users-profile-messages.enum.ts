export enum UsersProfileMessagesEnum {
  NOT_FOUND = 'profile.NOT_FOUND',
  CREATED_BEFORE = 'profile.CREATED_BEFORE',
  LIST_NOT_FOUND = 'profile.LIST_NOT_FOUND',
  INITIALIZE_TIME_EXPIRED = 'profile.INITIALIZE_TIME_EXPIRED',
}
